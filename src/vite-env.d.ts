/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** ISO instant baked in at build time by vite.config.ts — see the note there. */
declare const __BUILT_AT__: string;
